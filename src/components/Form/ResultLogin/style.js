import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    resultLogin: {
        flex:1,
        marginTop: 10,
        paddingTop: 0,
        borderRadius: 50,
        alignItems: "center",
        width: "100%",
    },

    information: {
        fontSize: 18,
        fontWeight: "bold",
        color: "#000",
    }
});

export default styles